GET / -> front/landing page

/* GET /admin -> show dashboard */
GET /admin -> now shows login popup, dashboard is moved to /dashboard

/* POST /admin -> login popup (binus.edu/admin) (need auth) */
auth is handled with request.authorization. this is not needed anymore

/dashboard -> show dashboard for admin

POST /jobpost -> admin add job posting

DEL /jobpost/:id -> admin del job posting

GET /results/:id  -> admin view filtered/ranked results

// rename from posting to talent to be less ambiguous?? ¯\_(ツ)_/¯
GET /talent/:id -> talent open submission box

POST /talent/:id -> post cv/resume

